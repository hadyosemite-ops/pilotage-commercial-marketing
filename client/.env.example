# Optionnel, uniquement si tu reactives "Se connecter avec Google" en plus du
# mot de passe (route /api/auth/google). Meme ID client que GOOGLE_CLIENT_ID cote
# server (celui-ci est public, affiche dans le navigateur, ce n'est pas un secret).
# VITE_GOOGLE_CLIENT_ID=xxxxxxxxxx.apps.googleusercontent.com
